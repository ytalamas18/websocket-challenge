apiVersion: v1
kind: Service
metadata:
  name: websocket-server
  labels:
    app: websocket-server
spec:
  type: ClusterIP
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080 # port of the application in the container
    name: http-ws
  selector:
    app: websocket-server